import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* This is an internal-only tool — no institution-facing traffic.
     Add config here as real needs come up (image domains, etc). */
};

export default nextConfig;
